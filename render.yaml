services:
  - type: web
    name: monitoramento-resultados
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /data
    disk:
      name: dados-resultados
      mountPath: /data
      sizeGB: 1
